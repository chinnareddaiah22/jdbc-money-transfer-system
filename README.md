# jdbc-money-transfer-system
A Java JDBC project that performs secure money transfer between bank accounts using Oracle Database. The application validates sender and receiver accounts, checks account balance, verifies PIN, performs debit and credit operations, and uses transaction management (commit and rollback) to maintain data consistency.
